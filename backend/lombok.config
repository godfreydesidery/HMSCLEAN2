# Lombok configuration (DIRECTIVE 1, ADR-0014 §3).
# Stop Lombok from looking for config files in parent directories.
config.stopBubbling = true
# Add @lombok.Generated to all generated members so MapStruct (and coverage tools) can detect them.
lombok.addLombokGeneratedAnnotation = true
