export const meta = {
  name: 'inc07-buildspec',
  description: 'Draft the FREEZABLE portion of the inc-07 build spec (07a admission/discharge/ward + 07b nursing charts + the freezable 07c fragments), every acceptance criterion anchored to a legacy file:line cite + the ratified decision, every owner-gated item explicitly marked frozen:false with its gate',
  phases: [
    { title: 'Draft', detail: 'specialists draft the freezable inc-07 spec sections in parallel' },
    { title: 'Check', detail: 'code-reviewer verifies no smuggled deviation; blocked items correctly marked' },
    { title: 'Assemble', detail: 'engagement-lead assembles the final freezable build spec' },
  ],
}

const DISC = 'd:/My_Works/HMS/HMSCLEAN2/docs/delivery/increments/07-inpatient-discovery'
const RECON = `${DISC}/01-RECONCILIATION-AND-SCOPE.md`
const DECISIONS = `${DISC}/03-DECISIONS-RATIFIED.md`
const DECRAW = `${DISC}/02-decisions-raw.json`
const RAW = `${DISC}/00-discovery-raw.json`
const PLANDOC = 'd:/My_Works/HMS/HMSCLEAN2/docs/delivery/increments/07-inpatient-nursing.md'
const NEWROOT = 'd:/My_Works/HMS/HMSCLEAN2/backend/src/main/java/com/otapp/hmis'

const GROUND = `
You are drafting part of the FREEZABLE inc-07 (Inpatient & Nursing) build spec for the Zana HMIS rebuild.
BINDING INPUTS (read them — do not work from memory):
- Ratified decisions register: ${DECISIONS} (THE authority on what is in-baseline vs parked-as-CR vs owner-gated). Also ${DECRAW} for the full per-question detail.
- Reconciliation & ground-truth legacy model: ${RECON}.
- Raw discovery (7 lane extracts with legacy com.orbix.api file:line cites + synthesis.groundTruthModel): ${RAW} — QUOTE these cites in acceptance criteria.
- The original planning doc (if present): ${PLANDOC} — treat as the HIGHEST-DRIFT doc of the engagement (~21 phantom); only build what the ratified decisions confirm.
- As-built HMSCLEAN2 code root: ${NEWROOT}. The inpatient module is an EMPTY STUB (only package-info.java). Build it from scratch. Reuse: clinical (PatientPrescriptionChart entity V27 + admission_uid loose refs pre-wired; ReferralPlan V28 with external_medical_provider_uid loose ref + admission_uid), billing::api (recordClinicalCharge/ChargeRequest, BillingQueries.getBillStatus, BillSettledEvent, PatientBill.settled), masterdata (Ward/WardType/WardBed/WardCategory V6, ServiceKind.WARD), registration (PatientType.DECEASED + PatientDeceasedEvent/PatientClosureListener), shared kernel (AuditRecorder ADR-0007, shared.documentnumber.DocumentNumberService, shared.event, ErrorCode, GlobalExceptionHandler), inc-08 pharmacy/inventory stock engine (decrementFefo package-private; PharmacyStockCredit published).

CHARTER: "modern design, exact process". Reproduce VERIFIED legacy behaviour VERBATIM in the baseline. NEVER write a deviation into the frozen baseline — those are parked as named CRs. Data-model modernization is allowed (double->BigDecimal NUMERIC; ULID uid + hidden BIGINT id per ADR-0014 §1; typed enums mapped 1:1 to legacy strings; Flyway). Process must be byte-identical to legacy.

HARD RULES from the ratified inc-07 decisions you MUST honour (frozen:false = BLOCKED, with the gate named):
- Q1 (MAR): NO MedicationAdministration/MAR entity. Confirmed phantom (no legacy basis; "dispensedAt" premise false). 07b builds the legacy free-text PatientPrescriptionChart dosing-note path ONLY. MAR is CR-07-MAR (parked, frozen:false if mentioned).
- Q2 (ward accrual): the accrual TOTAL/STATUS semantics are EXTRACTED parity facts and freezable as an unbound service method + parity oracle: total = (1+N) x WardType.price, N = completed rolling-24h intervals (ChronoUnit.HOURS.between(openedAt,now)>=24) while status in {IN-PROCESS,STOPPED}; admission bill UNPAID, accrued bills VERIFIED, insurance COVERED; each accrual closes the OPENED AdmissionBed + opens a new one resetting openedAt=now. The @Scheduled+ShedLock CRON MECHANISM is NET-NEW + BLOCKED (CR-07-Q2) behind ADR-0018 ratification + scheduling-infra authorization — mark every scheduler/cron criterion frozen:false. NO @Scheduled/ShedLock/Quartz exists today.
- Q3 (bed-claim lock): @Version-ONLY (inherits inc-08 CR-08-Q4 parked posture). Bed double-claim is prevented AT COMMIT by the optimistic lock (first commit wins EMPTY->WAITING; the loser's tx does not commit). The pessimistic lock is CR-07-Q3 and it BUNDLES the net-new ErrorCode.STALE_ENTITY + ObjectOptimisticLockingFailureException handler (BOTH ABSENT today). Do NOT spec STALE_ENTITY/409 as frozen. The concurrency IT asserts ONLY no-double-claim, NOT a 409.
- Q4 (approval): single-actor approver=creator (approvedBy=createdBy copied, PENDING->APPROVED), recorded via AuditRecorder. NO second-approver/SoD gate, NO SELF_APPROVAL_FORBIDDEN ErrorCode (CR-07-SoD parked).
- Q5 (deceased): reproduce the legacy open-admission re-admit block (existing PENDING/IN-PROCESS admission rejects 422) + the deceased terminal via the EXISTING PatientDeceasedEvent/PatientClosureListener. NO admit-time deceased guard, NO PATIENT_DECEASED ErrorCode, NO Patient.deceased boolean (CR-07-deceased-guard + CR-05 parked).
- Q6 (deceased modelling): DECEASED stays PatientType enum; inpatient deceased-finalize publishes the EXISTING PatientDeceasedEvent in-tx from ITS OWN inpatient service (NOT clinical ClosureService, which is OPD-only). No new event/listener/field, no inpatient->registration compile edge.
- Q7 (referral FK): reproduce the mandatory-provider RULE via a MANDATORY LOOSE external_medical_provider_uid (NOT NULL, @NotBlank, NO FK) matching clinical V28. Building ExternalMedicalProvider masterdata + real FK is CR-INC07-Q7 (parked). DISCH-5 "legacy was free-text" premise is WRONG.
- Q8 (WardTransfer): EXCLUDED. No WardTransfer entity / /transfer-ward / TRANSFERRED state. AdmissionStatus enum frozen at exactly PENDING/IN-PROCESS/STOPPED/HELD/SIGNED-OUT (CR-07-ward-transfer parked).
- Q9 (ward insurance): reproduce the active-flag-IGNORED eligibility VERBATIM (findByInsurancePlanAndCovered max-price-OR-exact-match loop; inactive-but-covered plan stays eligible) AND BUILD the load-bearing top-up split (COVERED principal at eligiblePlan.price + UNPAID 'Ward Bed / Room (Top up)' supplementary when (WardType.price-eligiblePlan.price)>0; no-top-up branch flips IN-PROCESS/OCCUPIED at admit, top-up branch stays PENDING/WAITING until top-up paid). Honouring the active flag is CR-07-Q9 (parked). Do NOT "fix" the query scope.
- Q10 (AdmissionBed leak): reproduce VERBATIM — final AdmissionBed left OPENED at discharge/sign-out; closed only at accrual rollover. WardBed and AdmissionBed stay DISTINCT entities. Closing-at-discharge is CR-07-Q10 (parked). Data-migration note: expect dangling OPENED rows.
- Q11 (consumable quirks): reproduce THREE latent bugs VERBATIM — (1) PatientInvoiceDetail.qty hard-coded to 1 (not chart.qty); (2) the consumable PatientCreditNote.reference mislabeled 'Canceled lab test'; (3) the unconditional parent-PatientInvoice delete (the j=j++ no-op at PatientResource.java:3070-3076 that cascade-wipes sibling details via orphanRemoval). Fixes are CR-07-Q11 (parked). QA asserts all three + the cascade-wipe of a shared multi-detail invoice as POSITIVE expectations.
- Q12 (consumable stock): BILLING-ONLY baseline (legacy touches NO stock). NO stock decrement, NO new seam, NO last-unit-409 AC. Stock-decrement is CR-07-consumable-stock (parked).
- Q13 (consumable ServiceKind): charge as ServiceKind.MEDICINE (no schema change; PriceLookup.resolve(plan,MEDICINE,medicineUid) reproduces cash=Medicine.price + MedicineInsurancePlan). This is OWNER-ROUTED — engagement-lead must CONFIRM. Reproducing the legacy bill-line literals billItem='Medication'/description='Consumable: <name>' requires CR-07-Q13-billing-display (ChargeRequest has NO billItem/description). Mark the literal-display criteria frozen:false; the MEDICINE pricing path itself is freezable once owner confirms. 07c must golden-master the cash amount quirk (legacy amount=unit price vs as-built price*qty) at 2dp.
- ALL ADRs are status 'Proposed' (docs/adr/README.md line 3). No ADR may be relied on to authorize net-new work until ratified. Owner-gated ratifications: ADR-0017 (Q3 lock), ADR-0018 (Q2 accrual — and its §Context false premise must be corrected by the solution-architect BEFORE ratification).
- AUDIT = shared.audit.AuditRecorder (SHA-256 chained, ADR-0007). NEVER Envers, NEVER device-fingerprint (confirmed phantom deps). Inpatient surface was UNGATED in legacy; deny-by-default authenticated() is approved ADR-0006 hardening (label net-new, NOT a parity assertion). ADMISSION-CREATE is the only legacy-valid inpatient privilege code (and it was commented out). Operation suffixes outside {ACCESS,ALL,CREATE,READ,UPDATE,DELETE,CANCEL,REJECT,VERIFY,APPROVE,ARCHIVE,ALLOW} are auto-purged — *-WRITE/*-DISCHARGE are purge-incompatible.
- Migrations start at Flyway V44 (inc-08 ended V43). Admission has NO legacy number/sequence — uid surrogate is fine.
Output structured data only.`

const SECTION_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['section', 'overview', 'acceptanceCriteria', 'blockedItems', 'cites'],
  properties: {
    section: { type: 'string' },
    overview: { type: 'string', description: 'markdown: what this slice builds + the legacy-accurate behaviour, with the sub-increment (07a/07b/07c) noted' },
    acceptanceCriteria: {
      type: 'array',
      items: {
        type: 'object', additionalProperties: false,
        required: ['id', 'criterion', 'legacyCite', 'decisionRef', 'frozen'],
        properties: {
          id: { type: 'string', description: 'e.g. AC-ADM-01' },
          criterion: { type: 'string', description: 'a testable Given/When/Then or assertion' },
          legacyCite: { type: 'string', description: 'the legacy com.orbix.api file:line that proves the expected behaviour, or "n/a — modern data-type only"' },
          decisionRef: { type: 'string', description: 'which ratified Q-decision authorizes this (e.g. Q9-baseline, Q2-parity-oracle)' },
          frozen: { type: 'boolean', description: 'true = freezable now; false = BLOCKED pending an owner/HDE/ADR gate (name the gate in the criterion)' },
        },
      },
    },
    blockedItems: { type: 'array', items: { type: 'string' }, description: 'items in this section that CANNOT freeze + which gate (CR-xx / ADR-xx ratification / owner confirm) blocks them' },
    cites: { type: 'array', items: { type: 'string' }, description: 'key legacy file:line cites used' },
  },
}

// Freezable inc-07 sections, each drafted by the right specialist.
const SECTIONS = [
  { key: 'prereq-clinical-api', agent: 'solution-architect',
    prompt: `SECTION (prereq): Cross-module seams inpatient needs, mirroring the inc-08 clinical::api / billing::api pattern (no cycles; ApplicationModules.verify() must stay green).
Spec: (1) the AdmissionStatus typed enum mapped 1:1 to legacy PENDING/IN-PROCESS/STOPPED/HELD/SIGNED-OUT (no TRANSFERRED, Q8); (2) a clinical::api published read+write port for PatientPrescriptionChart (entity is in clinical.domain V27 with admission_uid loose ref, package-private repo, NO api surface today) so the 07b dosing-note path and read surface work without a cycle (allowed edge inpatient -> clinical::api, or chart write owned where? — decide and justify); (3) the billing seam reuse: discharge bills-cleared gate computed live via existing BillingQueries.getBillStatus iteration (recommended, legacy-faithful) vs a NEW BillingQueries.billsOutstanding(admissionUid) query, PLUS a NEW inpatient settlement listener for the payment-driven PENDING->IN-PROCESS promotion (ConsultationSettlementListener ignores ward/registration bills — confirm and spec the new @TransactionalEventListener(BEFORE_COMMIT) on BillSettledEvent); (4) the deceased-finalize publishing the EXISTING PatientDeceasedEvent from the inpatient service in-tx (Q6, no new event, no inpatient->registration edge); (5) confirm Q13 routes consumable pricing via the existing billing::api recordClinicalCharge(kind=MEDICINE) and FLAG that reproducing billItem='Medication'/description='Consumable: <name>' needs CR-07-Q13-billing-display (mark frozen:false). Define published interfaces + DTOs (no entity leak) + Spring Modulith @NamedInterface declarations + the allowed-edge list.` },
  { key: 'admission-lifecycle', agent: 'backend-engineer',
    prompt: `SECTION (07a): Two-phase admission lifecycle + admit guards + payment-driven activation.
Spec doAdmission (legacy PatientServiceImpl.java:1701-2021): create admission PENDING, bed WAITING, an UNPAID ward-bed PatientBill at WardType.price (billItem 'Bed', desc 'Ward Bed / Room'), an AdmissionBed(OPENED, openedAt=now), flip patient.type=INPATIENT. ACTIVATION is PAYMENT-GATED: ONLY the full-insurance-cover branch (:1950-1963) activates to IN-PROCESS/bed OCCUPIED immediately; CASH and insurance-with-topup stay PENDING. The payment-driven promotion (PatientBillResource.java:352-365): paying the ward-bed bill promotes ALL the patient's PENDING admissions to IN-PROCESS + beds OCCUPIED and signs out IN-PROCESS consultations — spec this as the NEW inpatient settlement listener consuming BillSettledEvent. Admit guards in legacy order: existing PENDING/IN-PROCESS admission rejects 422 (PatientResource.java:5194-5200); bed must be active+EMPTY (PatientServiceImpl.java:1703-1711); NO deceased guard (Q5). Bed claim is @Version-only — concurrency IT asserts no-double-claim ONLY, no 409 (Q3, STALE_ENTITY frozen:false). Consultation->admission trigger is owned by inc-07. PENDING admissions appear in NO worklist; charting hard-rejects 'Admission not verified' (PatientServiceImpl.java:2062-2069). NO admission number/sequence (uid only).` },
  { key: 'ward-billing-topup', agent: 'backend-engineer',
    prompt: `SECTION (07a): Ward pricing — cash + insurance eligibility + the load-bearing top-up split (Q9).
Spec: cash ward bill at WardType.price. INSURANCE eligibility selects WardTypeInsurancePlan via findByInsurancePlanAndCovered(plan,true) by a max-price-OR-exact-plan-match loop (PatientServiceImpl.java:1795-1819) — the .active flag is IGNORED, an inactive-but-covered plan STAYS eligible (WardTypeInsurancePlanRepository.java:40); reproduce VERBATIM (honouring active is CR-07-Q9, frozen:false). If the eligible plan differs from the patient's own AND (WardType.price - eligiblePlan.price) > 0, create a supplementary 'Ward Bed / Room (Top up)' UNPAID PatientBill for the difference (:1880-1897). The no-top-up insurance branch activates IN-PROCESS/OCCUPIED at admit; the top-up branch stays PENDING/WAITING until the top-up bill is paid. NOTE the as-built deferred gap: PriceLookupImpl.java:30-38 does NOT implement the ward referral-override + top-up split — so inc-07 must BUILD this (it is load-bearing for the activation gate), either in the inpatient service or by re-homing into service_prices. Money is BigDecimal NUMERIC(19,2); reproduce legacy rounding at 2dp. Insurance ward bill is COVERED; admission bill UNPAID; accrued VERIFIED.` },
  { key: 'disposition-discharge-gate', agent: 'backend-engineer',
    prompt: `SECTION (07a): Three disposition entities + the hard bills-cleared discharge gate + single-actor approval.
Spec the THREE SEPARATE entities (NO typed kind discriminator, Q-disposition): DischargePlan (admission-only, flat narrative fields history/investigation/management/operationNote/icuAdmissionNote/generalRecommendation DischargePlan.java:38-46, NOT validated mandatory at create); ReferralPlan (admission AND consultation via XOR, mandatory loose external_medical_provider_uid NOT NULL @NotBlank no FK — Q7); DeceasedNote (admission AND consultation via XOR, validates patientSummary+causeOfDeath mandatory, uses split user-entered LocalDate date_ + LocalTime time_ DeceasedNote.java:40-46, NOT a server clock). Each has a free-text status default PENDING -> only APPROVED. The HARD bills-cleared gate is an inline service throw (NOT a flag, NOT a worklist filter), duplicated across get_discharge_summary (PatientResource.java:5342-5357), get_referral_summary (:5593-5603), get_deceased_summary (:5851-5882): iterate PatientInvoice->PatientInvoiceDetail->PatientBill.getStatus(), reject if ANY == 'UNPAID' OR 'VERIFIED'; applies to ALL admissions (NOT cash-only); insurance passes because COVERED isn't UNPAID/VERIFIED. NO billsCleared field. On PASS: APPROVE all invoices as a side effect, then (only if status==STOPPED/HELD) set SIGNED-OUT, free WardBed to EMPTY, reset Patient.type=OUTPATIENT/paymentType=CASH/insurancePlan=null (deceased sets type=DECEASED via the EXISTING PatientDeceasedEvent — Q6). Deceased path frees the bed EARLY at the HELD transition (saveDeceasedNote :5719-5732) and does NOT stamp dischargedBy/On/At. Approver ALWAYS = creator (approvedBy=createdBy copied :5378-5381) — single-actor, NO SoD (Q4, SELF_APPROVAL_FORBIDDEN frozen:false). AdmissionBed NEVER closed at discharge — reproduce the leak (Q10, close-at-discharge frozen:false). Record approvals via AuditRecorder.` },
  { key: 'nursing-charts', agent: 'backend-engineer',
    prompt: `SECTION (07b): The FIVE flat nursing-chart entities + the free-text dosing-note path (NO MAR).
Spec the FIVE entities (NOT six): PatientVital, PatientNursingChart (fluid-balance drainageOutput/fluidIntake/urineOutput AND care-activity feeding/changingPosition/bedBathing/randomBloodSugar/fullBloodSugar are free-text String COLUMNS PatientNursingChart.java:38-45, NOT separate entities), PatientNursingCarePlan (four free-text strings, NO ACTIVE/RESOLVED lifecycle), PatientNursingProgressNote (single free-text 'note', NO kind enum), PatientDressingChart (a BILLING record: NO WoundStatus/wound field; qty/paymentType/membershipNo + mandatory @OneToOne PatientBill + mandatory @ManyToOne ProcedureType; gated by a Dressing masterdata marker 'Procedure type is not listed as dressing'; savePatientDressingChart writes money PatientServiceImpl.java:2078-2245: UNPAID at ProcedureType.price, INSURANCE/admission->ProcedureTypeInsurancePlan covered->COVERED+PENDING invoice else admission->VERIFIED; delete raises PENDING credit note + cascade). Chart/note/care-plan saves are admission-ONLY inserts (hard-reject outpatient/outsider), require a Nurse, gate on admission status (PENDING reject 'Admission not verified'; IN-PROCESS proceed; else 'Patient already signed off' PatientServiceImpl.java:2622-2635). All charts deletable ONLY within a 24h window (PatientResource.java:3135-3138), DELETE-only (no edit path). PatientVital is a SINGLE-ROW upsert per context (last-wins, PatientResource.java:1043-1114) with free-text EMPTY->PENDING->SUBMITTED submit state machine + context-exclusivity gate; vitals support consultation/nonConsultation/admission (NOT admission-only) and uniquely have NO patient/nurse FK. The PatientPrescriptionChart dosing-note write path (entity exists clinical.domain V27): prescription must be GIVEN (legacy :2544), requires admission IN-PROCESS + nurse uid (:2564-2577); free-text dosing notes ONLY. NO MAR/MedicationAdministration/route/patientResponse/dispensedAt (Q1, all frozen:false / parked CR-07-MAR).` },
  { key: 'consumable-billing-only', agent: 'backend-engineer',
    prompt: `SECTION (07c, the freezable fragment): Consumable issue as a BILLING-ONLY event (Q12) + the three latent bugs (Q11) + accrual parity oracle (Q2 freezable part).
Spec savePatientConsumableChart (PatientServiceImpl.java:2250-2475): guards Medicine exists / qty>0 / Medicine registered as consumable ('Medicine is not listed as consumable') / nurse present / exactly-one context / admission status; create a PatientBill and persist PatientConsumableChart with free-text status hard-coded 'NOT-GIVEN' (never advanced; the only 'GIVEN' is a read-only delete guard). Bill status trichotomy: UNPAID (cash :2307-2319) | COVERED (MedicineInsurancePlan covered=true :2321-2395) | VERIFIED (cash-on-admission :2396-2465). NO stock read/decrement, NO negative-stock gate, NO lock, NO FEFO, NO last-unit-409 (Q12 — stock is CR-07-consumable-stock). Pricing via billing::api recordClinicalCharge(kind=MEDICINE) (Q13 — owner-confirm; reproducing billItem='Medication'/description='Consumable: <name>' needs CR-07-Q13-billing-display, mark frozen:false; golden-master the cash amount quirk legacy=unit-price vs as-built=price*qty at 2dp, frozen:false until Q13 confirmed). THE THREE LATENT BUGS reproduced VERBATIM (Q11): (1) PatientInvoiceDetail.qty hard-coded to 1 in BOTH branches (not chart.qty); (2) consumable delete raises a PENDING PatientCreditNote with reference mislabeled 'Canceled lab test'; (3) the unconditional parent-PatientInvoice delete (j=j++ no-op PatientResource.java:3070-3076 cascade-wipes sibling details via orphanRemoval) — assert all three + the shared-multi-detail-invoice cascade-wipe as POSITIVE expectations (fixes parked CR-07-Q11). PLUS the WARD-ACCRUAL PARITY ORACLE (Q2): an idempotent unbound accrueWardDay(admission) service method reproducing UpdatePatient.java:258-334 — for {IN-PROCESS,STOPPED} admissions whose OPENED AdmissionBed is >=24h old (ChronoUnit.HOURS.between(openedAt,now)>=24 :291-292) close that AdmissionBed + create a NEW VERIFIED ward-bed PatientBill at WardType.price + a NEW OPENED AdmissionBed (openedAt=now :294-334); de-dup branch closes multiple OPENED beds; accrual STOPS once status leaves {IN-PROCESS,STOPPED}; total=(1+N)xWardType.price. The @Scheduled+ShedLock CRON TRIGGER is frozen:false (CR-07-Q2, ADR-0018 ratification + scheduling-infra). Provide a parity-oracle test that walks chained 24h windows + the 5-min-poll-latency mechanic (chained N can be < floor(elapsed/24h)).` },
]

log(`Drafting ${SECTIONS.length} freezable inc-07 build-spec sections.`)

const drafted = await pipeline(
  SECTIONS,
  (s) => agent(`${GROUND}\n\n${s.prompt}`, { label: `draft:${s.key}`, phase: 'Draft', agentType: s.agent, schema: SECTION_SCHEMA }),
  (section, s) => {
    if (!section) return null
    return agent(
      `${GROUND}\n\nYou are the code-reviewer adversarially auditing a drafted inc-07 build-spec section "${s.key}" for charter compliance. Be ruthless.\nCheck EVERY acceptance criterion:\n1. Does it SMUGGLE a deviation into the FROZEN baseline? These MUST be absent or marked frozen:false with the correct gate: a typed DischargePlan.kind discriminator; WardTransfer/TRANSFERRED state; second-approver/SoD/SELF_APPROVAL_FORBIDDEN; admit-time deceased guard/PATIENT_DECEASED/Patient.deceased boolean; STALE_ENTITY or 409-on-bed-race as frozen; pessimistic lock as frozen; honouring WardTypeInsurancePlan.active; closing AdmissionBed at discharge; consumable stock decrement / FEFO / last-unit-409 / new decrement seam; FIXING any of the three Q11 bugs (qty=1, mislabeled credit-note ref, unconditional parent-invoice delete) instead of reproducing them; a real ExternalMedicalProvider FK; MAR/MedicationAdministration/route/patientResponse/dispensedAt; a @Scheduled/ShedLock cron as frozen; billItem='Medication'/description='Consumable: <name>' literals as frozen (needs CR-07-Q13-billing-display); a 6th nursing entity or separate FluidBalance/CareActivity entities; a PatientDressingChart wound/WoundStatus field; an edit path on nursing charts (DELETE-only); Envers/@Audited or device-fingerprint; the "one flat ward charge, no re-accrue" wrong premise; ADMISSION number/sequence; *-WRITE/*-DISCHARGE privilege codes; a 403-on-wrong-role parity assertion on formerly-ungated inpatient flows.\n2. Is every frozen:true criterion backed by a REAL legacy file:line cite (or a legitimate modern data-type change) AND authorized by a ratified decision?\n3. Are the BLOCKED items correctly marked frozen:false with the right gate (CR-07-Q2 cron / CR-07-Q3 lock+STALE_ENTITY / CR-07-Q13-billing-display / owner-confirm Q13)?\n4. Is the sub-increment (07a/07b/07c) correct?\nReturn UPHELD or REVISE with specific, actionable corrections.\n\nDRAFTED SECTION:\n${JSON.stringify(section, null, 2)}`,
      { label: `check:${s.key}`, phase: 'Check', agentType: 'code-reviewer',
        schema: { type: 'object', additionalProperties: false, required: ['section','verdict','reason','corrections'],
          properties: { section:{type:'string'}, verdict:{type:'string',enum:['UPHELD','REVISE']}, reason:{type:'string'}, corrections:{type:'array',items:{type:'string'}} } } },
    ).then(chk => ({ key: s.key, section, check: chk }))
  },
)

const sections = drafted.filter(Boolean)

phase('Assemble')
const spec = await agent(
  `${GROUND}\n\nYou are the engagement-lead assembling the FINAL freezable inc-07 build spec from the drafted + code-reviewer-checked sections. APPLY every REVISE correction before assembling (do not carry a smuggled deviation forward).\nProduce a single coherent build-spec document body (markdown) with: (1) a scope statement — what 07a freezes now, what 07b freezes now, and which 07c fragments are freezable (consumable billing-only path + accrual parity oracle) vs BLOCKED (the cron trigger, the billItem/description literals, the Q13 owner-confirm); (2) the prerequisite section (clinical::api prescription port + AdmissionStatus enum + the new inpatient settlement listener + the live bills-cleared computation + deceased-event reuse); (3) each section's overview + a numbered acceptance-criteria table (mark BLOCKED criteria clearly with their gate); (4) a consolidated BLOCKED-UNTIL list (every owner/ADR gate: ADR-0017 Q3, ADR-0018 Q2 + its §Context correction, CR-07-Q2 cron, CR-07-Q3 lock+STALE_ENTITY, CR-07-Q13-billing-display, owner-confirm Q13); (5) the V44+ migration list (admissions, admission_beds, the three disposition tables, the five nursing-chart tables, patient_consumable_charts, ward/service_prices SEED fixtures — note ZERO ward+service_prices seed exists today); (6) a Definition-of-Done for the freezable slice (mvn verify green incl. ModularityTest/ApplicationModules.verify, golden-master parity tests for ward billing + accrual oracle + the three Q11 quirks). Anchor every acceptance criterion to a legacy cite. Be precise that this is the EXACT-PROCESS baseline; name every parked CR. State the build order 07a -> 07b -> 07c.\n\nCHECKED SECTIONS:\n${JSON.stringify(sections, null, 2)}`,
  { label: 'assemble:inc07-spec', phase: 'Assemble', agentType: 'engagement-lead',
    schema: { type: 'object', additionalProperties: false,
      required: ['title','scope','specBody','blockedUntil','migrations','definitionOfDone','readyToFreeze'],
      properties: {
        title: { type: 'string' },
        scope: { type: 'string' },
        specBody: { type: 'string', description: 'the full markdown build-spec body' },
        blockedUntil: { type: 'array', items: { type: 'string' } },
        migrations: { type: 'array', items: { type: 'string' } },
        definitionOfDone: { type: 'array', items: { type: 'string' } },
        readyToFreeze: { type: 'string', description: 'which sections/sub-increments freeze now vs wait' },
      } } },
)

return { sections, spec }
